interface:
  display_name: "Make AI Assistant"
  short_description: "Make App AI chat, multi-session, file, and SSE integration"
  default_prompt: "Use $make-ai-assistant to integrate the Agent chat in a Make App using public package APIs, paginated Agent discovery, multi-session chat, file/image upload, SSE recovery, versioned Service routes, package-owned UI, and tests."
