interface:
  display_name: "Make Env Setup"
  short_description: "Prepare the local Make development environment"
  default_prompt: "Use $make-env-setup to prepare the Make toolchain, preserve any existing App runtime, verify makecli login, and initialize a folder only for a new App."
